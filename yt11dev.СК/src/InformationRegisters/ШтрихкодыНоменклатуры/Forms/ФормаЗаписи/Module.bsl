
&НаКлиенте
Процедура СК_НовыйШтрихкодВместо(Команда)
	
	ОчиститьСообщения();
	
	Если ЭтоВесовойТовар(Запись.Номенклатура, Запись.Упаковка) Тогда
		//Запись.Штрихкод = СформироватьШтрихкодEAN13ВесовогоТовара(); Штатно
		Запись.Штрихкод = СформироватьШтрихкодEAN13();
	Иначе
		Запись.Штрихкод = СформироватьШтрихкодEAN13();
	КонецЕсли;

КонецПроцедуры
