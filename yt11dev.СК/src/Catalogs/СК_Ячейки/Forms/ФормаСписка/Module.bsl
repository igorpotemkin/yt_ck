
&НаКлиенте
Процедура Генерировать(Команда)
	Форма = ПолучитьФорму("Справочник.СК_Ячейки.Форма.ФормаЯчеек");
	Форма.РежимОткрытияОкна = РежимОткрытияОкнаФормы.БлокироватьОкноВладельца;
	Форма.Открыть();

КонецПроцедуры

&НаКлиенте
Процедура ПечатьЯчеек(Команда)
	Форма = ПолучитьФорму("Справочник.СК_Ячейки.Форма.ФормаПечать");
	Форма.РежимОткрытияОкна = РежимОткрытияОкнаФормы.БлокироватьОкноВладельца;
	Форма.Открыть();
КонецПроцедуры
