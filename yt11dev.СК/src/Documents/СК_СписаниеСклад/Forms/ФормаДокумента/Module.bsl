
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если РольДоступна("ПолныеПрава") Тогда
		
		ЭтаФорма.Элементы.СобранныеТовары.ИзменятьСоставСтрок = Истина;
		ЭтаФорма.Элементы.СобранныеТовары.ТолькоПросмотр = Ложь;
			
		
	КонецЕсли;
	
КонецПроцедуры
