
&Перед("ПередЗаписью")
Процедура СК_ПередЗаписью(Отказ, Замещение) 
	Таб = ЭтотОбъект.Выгрузить(,"Номенклатура"); 
	Если Таб.Количество() >=1 Тогда
		Отказ = Обработки.СК_МобСклад.ПоискШтрихКодаПоНоменклатуре(Таб[0].Номенклатура);
	КонецЕсли;
	
КонецПроцедуры
